package pjw;
/* 1. 상속
 * 부모의 입력 값을 자식이 쓸 수 있다.
 * 부모는 여러명의 자식을 둘 수 있다.
 * class A extends B (A클래스는 자기 자신 B 클래스는 부모 클래스)
 * 2. 오버 로딩
 * 매개 변수 타입이 달라야 한다.
 * 매개변수 개수 가 달라야 한다
 * public int plus(int x, int y, int z)
 * public int plus(int x, int y)
 * public String plus(String x, String y)
 * 3. 오버라이딩
 * 오버라이딩은 상속과 관련이 있다. 즉, 상속을 해야 오버라이딩이 실행이 된다.
 * 메서드의 이름이 같아야 한다.
 * 매개변수가 같아야 한다.
 * 리턴 타입이 같아야 한다.
 * 4. 생성자와 메서드의 차이점
 * 생성자는 return 타입이 없는데 메서드는 return 타입이 있다. */
