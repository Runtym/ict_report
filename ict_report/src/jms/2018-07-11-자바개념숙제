1. 자바에서 상속에 대해서 정리해 서술 해주세요.
	상위클래스의 멤버변수와 메소드를 하위클래스가 가져다 쓸 수 있다.
	예로 부모라는 클래스와 자식이라는 클래스가 있고
	자식클래스가 부모클래스를 상속 받았다면,
	자식클래스는 부모클래스의 멤버변수와 메소드를 쓸 수 있다.
	
	A의 데이터를 B가 상속받는다면
	B는 A와B의 데이터를 쓸 수 있고,
	B의 데이터를 C가 상속받는다면
	C는 A,B,C의 데이터를 쓸 수 있음.
	
	
2. 오버로딩에 대해서 정리하여 서술해주세요.
	같은 이름의 메소드를 여러개 선언할 때, 혹은 상위클래스와 하위클래스에
	같은 이름의 메소드가 있을때 매개변수?(뭐라고 부르는지 잘 모르겠어요..)를 다르게 하여
	여러 유형의 메소드중에서 원하는 유형의 메소드를 호출하는 것.
	
	1.void test(int i)
	2.void test(String str)
	
	위와 같이 있을 때,
	test(1)은 1번 메소드 호출
	test("asd")는 2번 메소드 호출
	
	
3. 오버라이딩에 대해서 정리하여 서술해주세요.
	상위클래스에서 상속받은 메소드를 하위클래스에서 재정의 하는것.
		
	예로 test라는 이름의 메소드를 오버라이딩 한다고 가정하면
	상위클래스
	public int i = 3;
	public void test(){
		System.out.print(i);
		}
	
	하위클래스
	public String str = "대만 맞아라.";
	public void test(){
		System.out.print(i);
		System.out.println(str);
		}
	위와 같이 상속받은 메소드를 하위클래스에서 재정의하는 것을 오버라이딩이라 한다.
		
		
4. 생성자와 메서드와 차이점에 대해 정리하여 서술해주세요.
	솔직하게 말하자면 제대로 이해를 못해서 설명을 못하겠습니다.ㅜ
	정리글을 뒤져봐도 이해가 잘 안됩니다..
	아는것만 나열하자면..
	
	클래스와 이름이 같아야한다.
	접근제어자(private,protected,public)를 쓸 수 있다.
	오버로딩과 같이 매개변수?가 다른 여러개의 생성자를 선언할 수 있다.
	사용자정의 데이터타입초기화?( test t = new test();와 같은)를 할때에 호출된다.